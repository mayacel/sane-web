#!/bin/sh
set -eu
DIR="$HOME/Pictures/Screenshots"; mkdir -p "$DIR"
file="$DIR/$(date '+%Y-%m-%d_%H-%M-%S').png"
area="$(slurp)" || exit 0
grim -g "$area" "$file"
wl-copy < "$file"
notify-send 'Screenshot' 'Área salva e copiada.' 2>/dev/null || true
