#!/bin/sh
set -eu
DIR="$HOME/Pictures/Screenshots"; mkdir -p "$DIR"
file="$DIR/$(date '+%Y-%m-%d_%H-%M-%S').png"
grim "$file"
wl-copy < "$file"
notify-send 'Screenshot' 'Tela salva e copiada.' 2>/dev/null || true
